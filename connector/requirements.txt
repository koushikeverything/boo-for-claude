# Runtime deps for the LIVE Boo connector. The core logic + all tests run WITHOUT these
# (stdlib-only), so CI/quality gates need nothing installed. Install these only to serve live.
mcp>=1.2.0            # MCP server SDK (Streamable HTTP transport)
anyio>=4.0.0         # async runtime used by the MCP server
starlette>=0.37.0    # ASGI app: OAuth routes + MCP mount
uvicorn>=0.30.0      # ASGI server
httpx>=0.27.0        # live Google API + token endpoint calls
cryptography>=42.0.0 # optional AES-256-GCM crypto backend (BOO_CRYPTO_BACKEND=aesgcm)
# For production Postgres instead of SQLite, add: psycopg[binary]>=3.1
